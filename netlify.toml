[build]
  # Monorepo için root'ta install yap, sadece necrodev'i build et
  # Bu dosya necrodev için - root'ta deploy edildiğinde necrodev açılır
  base = "."
  command = "pnpm run build:netlify"
  publish = "apps/necrodev/.next"

[build.environment]
  NODE_VERSION = "18"
  PNPM_VERSION = "8.12.0"
  # Monorepo için workspace ayarları
  PNPM_FLAGS = "--filter @necroverse/necrodev..."

[[plugins]]
  package = "@netlify/plugin-nextjs"
  # Next.js plugin otomatik olarak Next.js'i algılar ve build eder
  # Monorepo için otomatik olarak apps/necrodev dizinindeki Next.js'i algılar

# Next.js plugin otomatik redirects ekler, ama yine de ekleyelim
[[redirects]]
  from = "/*"
  to = "/.netlify/functions/next"
  status = 200
