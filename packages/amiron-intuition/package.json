{
  "name": "@amiron/intuition",
  "version": "0.1.0",
  "description": "Amiron Intuition GUI Framework",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "(pnpm --filter @amiron/pal build || true) && (tsc --build || true)",
    "clean": "rimraf dist",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@amiron/pal": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "rimraf": "^5.0.5"
  }
}
